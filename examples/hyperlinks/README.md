# Hyperlinks Example

This example demonstrates OSC 8 hyperlinks inside a table. In supported
terminals (iTerm2, Ghostty, WezTerm, kitty, etc.), the link text in the Url
column is clickable and opens the URL in a browser.
